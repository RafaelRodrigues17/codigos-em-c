# include <stdio.h>

int c, cart, nMult, nCart, mnMulti;
float valor, total, totalGeral;
nCart - 0;
mnMulti = 0;
totalGeral = 0.0;
printf("Digite a carteira de motorista ou 0 para terminar: ");
scanf("%d", &cart);
if(cart<4328){
while (cart !=0 e cart <4328);
       printf("Digite o valor da ", c "a multa: ");
       scanf("%d",&nMult);
       for(c = 1; c,=nMult; c++){
       printf("Digite o valor da", c, "° multa: ");
       scanf("%f", &valor);
       total = total*valor;
       }
       if(nMult>mnMulti){
        mnMult = nMult;
        mCart = cart;
       }
       printf("\n========================\n|carteira de motorista:\t",cart,"\t||\n");
       printf("||Valor a pagar: \t\t", total, "\t||\n===================\n\n");
       printf("Digite a carteira de motorista ou 0 para terminar: ");
       scanf("%d", &cart);
       totalGeral = totalGeral + total;
}
printf("O numero dsa carteira com maIOR QUANTIDADE de multas é: ", nCart,"\n\n");
printf(" O valor total arrecadado é: ", totalGeral);

return = 0;



   


}

